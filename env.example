# Production: copy to .env and set. Required for session and credential encryption.
# SESSION_SECRET: strong random string (session + fallback encryption key).
# ENCRYPTION_KEY: strong random string used to encrypt stored credentials (recommended).
# BASE_URL: your app URL (e.g. https://your-domain.com). Used for OAuth redirect.
SESSION_SECRET=
ENCRYPTION_KEY=
BASE_URL=http://localhost:3000

# Optional server
PORT=3000

# Optional: set Azure/OpenAI here instead of in the app (app input overrides for that session)
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# AZURE_TENANT_ID=common
# OPENAI_API_KEY=
